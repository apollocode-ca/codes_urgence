import 'package:flutter/material.dart';

class StandaloneWidget extends StatelessWidget {
  const StandaloneWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Center();
  }
}
